<!DOCTYPE html>
<html>

    <head>
        <meta charset="utf-8">
        {{>csrf}}
        {{>grader_meta}}
        {{>stylesheets}}
        <title>{{title}}</title>
        {{>bootstrap_scripts}}
        {{>scripts}}
    </head>

    <body>
        {{>navigation}}
        <div class="container">
            {{{body}}}
        </div>
    </body>

</html>
